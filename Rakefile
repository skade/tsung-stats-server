require 'rubygems'
gem 'inochi', '~> 0'
require 'inochi'

Inochi.rake :TsungStatsServer do |gem|
  # TODO: additional gem configuration.
  # NOTE: this is completely optional!
  #
  # gem.extensions = ['foo.so']
  gem.files += ['sinatra', 'rubyzip', 'markaby']
end
